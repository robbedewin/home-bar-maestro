When also conda is activated you get an (venv) (base) prefix in the terminal.
# To remove the (base) prefix, run:
deactivate //for venv
conda deactivate //for conda

and then reactivate the venv:
.venv/Scripts/Activate.ps1